# K 线滚轮与导航实施计划

1. 新增纯函数 `chart-navigation.ts`，根据动作计算并钳制 Lightweight Charts 的可视逻辑范围；先写失败测试。
2. 扩展图表组件测试夹具，验证图表不截获滚轮，并验证四个主图导航按钮驱动可视区变化。
3. 修改 `ProfessionalCandlestickChart.tsx`：
   - 使用对象形式配置滚轮、拖拽和触控行为；
   - 提升主/副图初始高度；
   - 增加右下角导航按钮；
   - 包装顶部 sticky 数据区域。
4. 修改 `terminal.css`，完成吸顶、控制器定位、响应式和交互态样式。
5. 运行聚焦测试、全量测试、生产构建与浏览器页面检查。
